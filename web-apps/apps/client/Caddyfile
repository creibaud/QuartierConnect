:3000 {
	encode gzip

	@static path_regexp \.(js|css|woff2?|ttf|svg|png|ico)$
	header @static Cache-Control "public, max-age=31536000, immutable"

	handle_path /api/* {
		reverse_proxy api:5000
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
